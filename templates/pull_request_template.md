<!-- Briefly describe what this PR changes and link the issue (e.g. "Closes #NN"). -->

## Summary

-

## Checklist

- [ ] Tests pass locally and the linter/analyzer is clean.
- [ ] User-facing docs updated if this PR changes user-facing labels or flows.
      <!-- PLACEHOLDER: if the project has a docs-lint or "manual" test that
      catches renamed/removed labels automatically, name it here; note that
      behavioural changes (how a feature works, not just its label) still
      need a manual doc edit even when the lint passes. -->
- [ ] If a test level was intentionally skipped (see Definition of Done in
      CLAUDE.md), say which and why below. Do not stay silent about it.
